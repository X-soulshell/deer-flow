# DeerFlow 手动安装与开发指南（中文版）

本文档说明如何在 Windows 本地环境中，不使用 `make`、不使用 Docker，手动安装并启动 DeerFlow 的前端、Gateway、LangGraph，以及如何配置 MCP。

## 一、适用场景

适用于以下需求：

- 不想依赖 `make`
- 不想使用 Docker
- 希望分别进入前后端目录手动安装和启动
- 希望在本地调试前端、Gateway、LangGraph
- 希望理解 `config.yaml` 与 `extensions_config.json` 的区别
- 希望手动配置 MCP 工具

## 二、项目结构说明

DeerFlow 本地开发通常涉及以下几个部分：

- `frontend`：前端界面，默认端口 `3000`
- `backend`：后端代码目录
- `Gateway`：FastAPI 网关服务，默认端口 `8001`
- `LangGraph`：Agent 运行服务，默认端口 `2024`
- `Nginx`：统一反向代理入口，默认端口 `2026`

如果你不使用 Nginx，也可以直接分别启动前端、Gateway、LangGraph。

## 三、安装前准备

### 1. 安装 Python

后端要求 Python 版本不低于 `3.12`。

### 2. 安装 Node.js

建议安装较新的 Node.js 版本，并确保可用：

```powershell
node -v
npm -v
```

### 3. 安装 pnpm

如果尚未安装：

```powershell
npm install -g pnpm
```

### 4. 安装 uv

后端使用 `uv` 管理 Python 依赖。如果尚未安装，请先安装 `uv`。

安装完成后可验证：

```powershell
uv --version
```

## 四、手动安装依赖

### 1. 安装前端依赖

```powershell
cd h:\deer-flow\frontend
pnpm install
```

### 2. 安装后端依赖

```powershell
cd h:\deer-flow\backend
uv sync
```

## 五、主配置文件 config.yaml

DeerFlow 的主配置文件是 `config.yaml`。

推荐位置：

```text
h:\deer-flow\config.yaml
```

也可以放在：

```text
h:\deer-flow\backend\config.yaml
```

但推荐放在项目根目录。

### 1. 创建配置文件

如果项目中已有示例文件，可复制后修改：

```powershell
cd h:\deer-flow
Copy-Item config.example.yaml config.yaml
```

### 2. config.yaml 主要负责什么

`config.yaml` 主要负责 DeerFlow 的主运行配置，例如：

- 模型配置
- API Key
- sandbox 配置
- memory 配置
- skills 目录配置
- tool_groups 等主配置

### 3. 注意

`config.yaml` **不是** MCP Server 列表的主要配置文件。

MCP 配置使用的是单独的：

```text
h:\deer-flow\extensions_config.json
```

## 六、无 Nginx 的本地开发方式

如果你放弃 Nginx，本地建议直接启动三个服务：

1. LangGraph
2. Gateway
3. Frontend

### 1. 启动 LangGraph

```powershell
cd h:\deer-flow\backend
uv run langgraph dev --no-browser --allow-blocking --n-jobs-per-worker 10 --server-log-level info
```

说明：

- `--no-browser`：不自动打开浏览器
- `--allow-blocking`：避免本地开发时出现 `Blocking call to os.getcwd` 一类错误
- `--n-jobs-per-worker 10`：与项目脚本默认值一致
- `--server-log-level info`：输出信息级日志

### 2. 启动 Gateway

```powershell
cd h:\deer-flow\backend
$env:PYTHONPATH="."
$env:CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
uv run uvicorn app.gateway.app:app --host 0.0.0.0 --port 8001
```

说明：

- `PYTHONPATH=.`：确保后端包可正确导入
- `CORS_ORIGINS`：允许前端开发地址访问 Gateway
- Gateway 默认端口为 `8001`

### 2.1 app.py 中的跨域处理说明

如果不走 Nginx，而是让前端直接访问 `http://localhost:8001`，那么 Gateway 必须自己处理跨域。

当前项目已经在 `backend/app/gateway/app.py` 中加入了 FastAPI 的 `CORSMiddleware`，核心逻辑是：

- 从 `get_gateway_config()` 读取 `cors_origins`
- 在 `create_app()` 中调用 `app.add_middleware(CORSMiddleware, ...)`
- 允许前端开发地址访问 Gateway 的接口

也就是说，无 Nginx 开发模式下，`app.py` 需要负责处理浏览器对以下接口的跨域访问，例如：

- `/api/skills`
- `/api/memory`
- `/api/mcp/config`
- `/api/models`

如果没有这层中间件，浏览器从 `http://localhost:3000` 访问 `http://localhost:8001` 时，就会出现 CORS 报错。

因此，启动 Gateway 时除了运行 `uvicorn`，还要确保 `CORS_ORIGINS` 环境变量包含前端地址，例如：

```powershell
$env:CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
```

如果你后续修改了前端端口或访问域名，也要同步更新这个环境变量。

### 3. 启动 Frontend

```powershell
cd h:\deer-flow\frontend
$env:NEXT_PUBLIC_BACKEND_BASE_URL="http://localhost:8001"
$env:NEXT_PUBLIC_LANGGRAPH_BASE_URL="http://localhost:2024"
pnpm dev
```

说明：

- `NEXT_PUBLIC_BACKEND_BASE_URL`：前端请求 Gateway
- `NEXT_PUBLIC_LANGGRAPH_BASE_URL`：前端请求 LangGraph
- 前端默认运行在 `http://localhost:3000`

## 七、如果使用 Nginx

如果你仍然想使用项目原本的统一入口模式，则通常会启动：

- LangGraph：`2024`
- Gateway：`8001`
- Frontend：`3000`
- Nginx：`2026`

Nginx 会统一代理前端页面、Gateway API 和 LangGraph API。

但如果只是本地开发调试，通常可以先不启用 Nginx。

## 八、MCP 配置说明

### 1. MCP 配置文件位置

MCP Server 配置不在 `config.yaml` 中，而在：

```text
h:\deer-flow\extensions_config.json
```

### 2. MCP 配置文件作用

`extensions_config.json` 用于配置：

- MCP servers
- skills 的启用状态

### 3. 当前已配置的 MCP

当前项目中已经配置了一个 `filesystem` MCP，内容如下：

```json
{
  "mcpServers": {
    "filesystem": {
      "enabled": true,
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "h:/deer-flow"
      ],
      "env": {},
      "url": null,
      "headers": {},
      "oauth": null,
      "description": "Provides filesystem access within the DeerFlow workspace"
    }
  },
  "skills": {}
}
```

### 4. 这个 filesystem MCP 能做什么

它可以让 Agent 通过 MCP 访问你允许的目录。

当前允许访问的目录是：

```text
h:/deer-flow
```

适合用于：

- 浏览项目目录
- 读取文件
- 分析本地代码
- 在允许范围内进行文件相关操作

### 5. MCP 生效方式

修改 `extensions_config.json` 后，需要重启相关服务，通常至少重启：

- LangGraph
- Gateway

这样 DeerFlow 才会重新加载 MCP 配置。

## 九、如何让“工具”页显示 MCP

前端“设置 -> 工具”页面显示的是 MCP Server 列表。

如果该页面为空，通常说明：

- `extensions_config.json` 不存在
- 文件中没有配置 `mcpServers`
- 配置存在但没有成功加载
- 相关服务尚未重启

因此，排查顺序建议为：

1. 检查 `h:\deer-flow\extensions_config.json` 是否存在
2. 检查其中是否有 `mcpServers`
3. 检查 MCP 是否设置为 `enabled: true`
4. 重启 LangGraph 和 Gateway
5. 刷新前端页面

## 十、推荐的手动开发顺序

建议按以下顺序启动：

### 方案 A：无 Nginx

1. 启动 LangGraph
2. 启动 Gateway
3. 启动 Frontend
4. 打开 `http://localhost:3000`

### 方案 B：带 Nginx

1. 启动 LangGraph
2. 启动 Gateway
3. 启动 Frontend
4. 启动 Nginx
5. 打开 `http://localhost:2026`

## 十一、常见问题

### 1. LangGraph 启动时报 BlockingError

如果出现类似错误：

```text
Blocking call to os.getcwd
```

请使用：

```powershell
uv run langgraph dev --no-browser --allow-blocking
```

### 2. 前端访问 Gateway 出现 CORS 报错

请确保启动 Gateway 时设置了：

```powershell
$env:CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
```

### 3. 工具页没有显示任何内容

优先检查：

- `extensions_config.json` 是否存在
- 是否配置了 `mcpServers`
- 服务是否已重启

### 4. config.yaml 和 extensions_config.json 有什么区别

可以简单理解为：

- `config.yaml`：DeerFlow 主配置
- `extensions_config.json`：MCP 与扩展配置

## 十二、最小可用启动命令汇总

### 安装

```powershell
cd h:\deer-flow\frontend
pnpm install

cd h:\deer-flow\backend
uv sync
```

### 启动 LangGraph

```powershell
cd h:\deer-flow\backend
uv run langgraph dev --no-browser --allow-blocking --n-jobs-per-worker 10 --server-log-level info
```

### 启动 Gateway

```powershell
cd h:\deer-flow\backend
$env:PYTHONPATH="."
$env:CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
uv run uvicorn app.gateway.app:app --host 0.0.0.0 --port 8001
```

### 启动 Frontend

```powershell
cd h:\deer-flow\frontend
$env:NEXT_PUBLIC_BACKEND_BASE_URL="http://localhost:8001"
$env:NEXT_PUBLIC_LANGGRAPH_BASE_URL="http://localhost:2024"
pnpm dev
```

## 十三、后续建议

如果你后续还想继续扩展 MCP，可以考虑再配置：

- GitHub MCP
- PostgreSQL MCP
- HTTP/SSE 类型的远程 MCP

但对于本地开发来说，先使用 `filesystem` MCP 是最容易验证和最稳定的方案。
